# 单词航岛 Calm Adventure Design System

版本：1.0  
更新日期：2026-09-20  
适用产品：K12 英语单词速记微信小程序  
主要用户：小学三年级学生及其家长  

## 1. 设计语言概述

“单词航岛”采用 **清爽冒险（Calm Adventure）** 设计语言：以清晰、克制、低认知负担的交互结构承载轻量的岛屿冒险氛围。

设计语言由两类参考融合而成：

- Apple 式清晰与克制约占 70%：单屏单任务、明确层级、充足留白、有限颜色、少阴影、大触控目标。
- Nintendo 式冒险与趣味约占 30%：岛屿、航程、伙伴角色、星星、徽章和明确的前进反馈。

本规范不照搬任何品牌的具体视觉资产。Nintendo 的金属面板、像素字体和密集布局不适用于本产品；Apple 的商业展示结构也不直接套用。所有规则均围绕儿童学习、家长信任和微信小程序场景重新定义。

一句话原则：

> 用清晰克制的学习界面承载轻量冒险感，让孩子始终知道下一步做什么，也让家长相信这是一款认真、可靠的学习工具。

## 2. 设计目标

### 2.1 核心目标

1. 学生首次使用时，无需成人解释即可开始并完成一次学习航程。
2. 每屏只有一个主要学习目标，降低阅读、判断和操作负担。
3. 趣味元素帮助理解内容和感知进度，不干扰英文单词、标准发音和词义辨认。
4. 错误反馈保持积极、可执行，不羞辱、不惩罚、不制造失败感。
5. 家长界面清晰可信，能够快速理解学习投入、掌握情况和下一步建议。
6. 视觉系统可跨教材、年级和学段扩展，不依赖湘少版或三年级专属元素。

### 2.2 非目标

- 不设计成游戏大厅、金币商城或重度养成产品。
- 不使用公开排名、抽奖、宝箱、连续签到惩罚或无限任务。
- 不设计成传统教辅软件的高密度蓝白表格界面。
- 不采用过度幼态、五彩堆叠的幼儿园视觉风格。
- 不让冒险包装压过英文单词、标准发音和学习反馈。
- 不在儿童学习流程中展示价格、订单、催付或家长设置。

## 3. 核心设计原则

### 3.1 一屏一件事

学习过程中，每屏只处理一个明确动作，例如：

- 听标准音；
- 看图记意思；
- 选择正确图片；
- 跟读单词；
- 查看本次反馈；
- 完成航程。

同一屏不得出现两个同等级主按钮。辅助操作应弱化为文字按钮、图标按钮或次级按钮。

### 3.2 趣味来自内容，而非界面噪声

趣味主要由以下内容承担：

- 岛屿地图和航程推进；
- 谐音故事插图；
- 小型伙伴角色；
- 星星、徽章和插画碎片；
- 完成学习时的短庆祝动画。

按钮、弹窗、导航和表单保持简单稳定，不使用无意义的装饰、纹理或炫技动画。

### 3.3 暖色代表前进

暖色只表达行动、奖励和推进：

- 橙黄色：开始、继续、确认、完成、星星和航程推进；
- 蓝色：学习结构、标准音、当前状态和可信信息；
- 绿色：正确、掌握和完成；
- 红色：真实错误、危险操作和破坏性确认。

不要用橙黄色装饰非交互区域，以免削弱主按钮的辨识度。

### 3.4 先鼓励，再纠正

错误反馈必须告诉孩子下一步如何做，避免只给结论。

推荐文案：

- “再看看这张图。”
- “差一点，再听一次。”
- “注意最后一个音，再试试看。”
- “这次没有听清。”
- “先继续航行，稍后再练。”

禁止文案：

- “答错了。”
- “太差了。”
- “挑战失败。”
- “你落后了。”
- “连续学习中断。”

### 3.5 明确结束

每日学习完成后应明确告诉孩子任务已经结束，并建议离开屏幕休息。完成页不默认诱导继续刷题。

## 4. 色彩系统

### 4.1 基础色 Token

| Token | 色值 | 用途 |
|---|---|---|
| `color-brand-ocean` | `#3478B8` | 品牌海洋蓝、导航选中、标准音、当前进度 |
| `color-brand-ocean-dark` | `#255F94` | 蓝色按下状态、深色强调 |
| `color-brand-ocean-light` | `#DCEEF8` | 浅蓝信息区、进度底色、次级选中 |
| `color-action` | `#FFB23F` | 主要行动、继续、完成、星星 |
| `color-action-pressed` | `#F29A24` | 主按钮按下状态 |
| `color-action-soft` | `#FFF1D2` | 奖励背景、轻提示背景 |
| `color-ink` | `#18324B` | 标题、正文、主按钮文字 |
| `color-ink-secondary` | `#66798B` | 次要说明、辅助信息 |
| `color-canvas` | `#F3F8FC` | 页面主背景 |
| `color-surface` | `#FFFFFF` | 卡片、弹窗、选项背景 |
| `color-border` | `#DCE5EC` | 普通边框和分隔线 |
| `color-success` | `#46A875` | 正确、已掌握、完成 |
| `color-success-soft` | `#E3F4EB` | 正确反馈背景 |
| `color-warning` | `#D88A22` | 需要注意但不阻断的状态 |
| `color-error` | `#D94C4C` | 真实错误和危险操作 |
| `color-error-soft` | `#FCE8E8` | 错误提示背景 |
| `color-disabled` | `#B8C4CD` | 禁用文字和图标 |

### 4.2 使用比例

建议页面整体比例：

- 70%：白色和浅天空背景；
- 20%：海洋蓝及浅蓝；
- 10%：橙黄、绿色及其他状态色。

不同岛屿可以改变插画环境色，但主要按钮、标准音和状态反馈颜色必须保持一致。

### 4.3 颜色规则

- 主按钮优先使用 `color-action`，文字使用 `color-ink`。
- 标准音、重听和慢速音频统一使用海洋蓝。
- 正确答案不能只依靠绿色表示，必须同时出现图标或文字。
- 错误不能只依靠红色表示，必须提供原因和下一步动作。
- 单个页面原则上不同时出现超过三种高饱和颜色。
- 正文文字与背景应满足 WCAG AA 对比度要求。

## 5. 字体系统

### 5.1 字体栈

微信小程序优先使用系统字体，减少包体和跨设备差异：

```css
font-family: -apple-system, BlinkMacSystemFont, "PingFang SC",
  "Microsoft YaHei", "Helvetica Neue", Arial, sans-serif;
```

首版不引入像素字体、手写字体或复杂儿童字体。品牌个性通过插画、色彩和语气表达。

### 5.2 儿童学习界面

| Token | 字号 | 字重 | 行高 | 用途 |
|---|---:|---:|---:|---|
| `type-journey-title` | 28px | 700 | 1.25 | 今日航程、完成标题 |
| `type-word` | 38px | 700 | 1.15 | 当前英文单词 |
| `type-meaning` | 22px | 600 | 1.35 | 中文核心词义 |
| `type-card-title` | 20px | 600 | 1.35 | 学习卡片标题 |
| `type-body-child` | 18px | 400 | 1.55 | 谐音故事和主要提示 |
| `type-button` | 18px | 600 | 1.2 | 主按钮文字 |
| `type-caption-child` | 14px | 400 | 1.5 | 记忆暗号说明、辅助信息 |

### 5.3 家长界面

| Token | 字号 | 字重 | 行高 | 用途 |
|---|---:|---:|---:|---|
| `type-page-title` | 24px | 600 | 1.3 | 家长区页面标题 |
| `type-data` | 28px | 600 | 1.2 | 关键数据 |
| `type-section-title` | 17px | 600 | 1.4 | 卡片和区块标题 |
| `type-body` | 16px | 400 | 1.55 | 正文和说明 |
| `type-caption` | 13px | 400 | 1.5 | 辅助文字和时间信息 |

### 5.4 排版规则

- 英文单词可使用较大字号和更高字重；中文故事不应整段加粗。
- 标题最多两行，按钮文字原则上保持一行。
- 中文正文每行建议控制在 18–24 个汉字。
- 不使用全大写英文作为儿童端主要标签。
- 数字与单位之间保持清晰关系，例如“约 5 分钟”。

## 6. 间距与布局

### 6.1 间距 Token

采用 8px 基础网格：

| Token | 数值 | 用途 |
|---|---:|---|
| `space-1` | 4px | 图标与短标签 |
| `space-2` | 8px | 紧密关联内容 |
| `space-3` | 12px | 选项内部、小组件 |
| `space-4` | 16px | 常规内容间距 |
| `space-5` | 24px | 卡片内边距 |
| `space-6` | 32px | 主要模块之间 |
| `space-7` | 40px | 大区块和焦点内容 |
| `space-8` | 48px | 页面级垂直留白 |

### 6.2 页面布局

- 页面左右安全边距：16–20px。
- 儿童学习页采用单列居中布局。
- 主学习内容应处于首屏视觉中心。
- 底部主操作区域需避开微信系统安全区。
- 小屏设备上优先保持按钮、单词和插图尺寸，压缩装饰留白。
- 页面滚动不是儿童学习流程的默认交互；单个学习步骤应尽量在一屏内完成。

## 7. 形状与层级

### 7.1 圆角 Token

| Token | 数值 | 用途 |
|---|---:|---|
| `radius-small` | 8px | 小标签、状态块 |
| `radius-medium` | 14px | 普通选项、输入框 |
| `radius-card` | 20px | 主要学习卡片、报告卡片 |
| `radius-pill` | 999px | 主按钮、关键行动 |
| `radius-circle` | 50% | 音频、录音和图标按钮 |

胶囊形只用于明确可操作元素，不用于所有标签和卡片。

### 7.2 边框与阴影

- 普通卡片使用背景色变化或 1px `color-border` 边框区分层级。
- 默认卡片不使用明显阴影。
- 插画、漂浮角色和模态弹窗可使用柔和阴影。
- 不使用多层阴影、霓虹光效或强拟物高光。

建议弹窗阴影：

```css
box-shadow: 0 12px 32px rgba(24, 50, 75, 0.14);
```

## 8. 图标与插画

### 8.1 图标规范

- 使用圆润但结构清晰的线性图标。
- 建议线宽为 2px，关键状态可使用面性图标。
- 同一页面不得混用多种图标风格。
- 图标必须配合文字或明确语境，不用抽象图标承载关键学习指令。
- 星星、音频、麦克风、返回、关闭等高频图标保持全产品一致。

### 8.2 插画规范

推荐风格：

- 2D 平面插画；
- 柔和轮廓和少量纹理；
- 中等饱和度；
- 主体明确、动作清楚；
- 一个画面只表达一个记忆关系；
- 角色积极友好，不过度夸张。

谐音图片必须优先满足“三秒内看懂”，其次才是视觉精致度。

禁止：

- 写实 AI 儿童人物；
- 复杂背景和无关装饰；
- 大量文字嵌入图片；
- 暴力、羞辱、恐怖或成人化情节；
- 可能强化错误英语发音的汉字拟声视觉；
- 与真实教材插图高度相似但未经授权的画面。

## 9. 核心组件

### 9.1 主按钮 `button-primary`

- 背景：`color-action`；
- 文字：`color-ink`；
- 高度：52–56px；
- 圆角：`radius-pill`；
- 水平内边距：24px；
- 字号：18px，字重 600；
- 最小宽度：160px，首页主按钮可占容器全宽；
- 按下状态：背景切换为 `color-action-pressed`，缩放至 0.97；
- 禁用状态：降低饱和度，但保持文字可读。

一个页面只能有一个主按钮。

### 9.2 次级按钮 `button-secondary`

- 白色或透明背景；
- 海洋蓝文字和 1px 海洋蓝边框；
- 高度不低于 44px；
- 圆角：`radius-pill`；
- 用于“再听一次”“稍后再练”等辅助动作。

### 9.3 标准音按钮 `button-audio`

- 44–52px 圆形按钮；
- 海洋蓝背景或浅蓝背景加深蓝图标；
- 使用扬声器图标；
- 播放时显示轻量脉冲或声波；
- 在谐音故事和发音反馈页面始终可见且位置稳定。

### 9.4 录音按钮 `button-record`

- 64–72px 圆形按钮；
- 默认使用海洋蓝；
- 录音中使用扩散圆环，不能只依靠颜色说明状态；
- 同时展示“正在听”或“松开完成”等文字提示；
- 网络或权限异常时不显示失败分数。

### 9.5 学习卡片 `card-learning`

- 白色背景；
- 圆角：20px；
- 内边距：24px；
- 默认无明显阴影；
- 主要内容居中；
- 同一卡片内最多出现一个主要学习动作。

### 9.6 图片选项 `option-image`

- 推荐采用 2×2 网格或单列大卡片；
- 图片区域占选项面积约 70%；
- 点击区域最小为 64×64px；
- 默认使用浅边框；
- 选中使用蓝色 2px 描边；
- 正确使用绿色描边、正确图标和文字；
- 错误时轻微左右位移或降低选项强调，不用红色覆盖整张图片。

### 9.7 进度条 `progress-journey`

- 表达“航程完成度”，不表达与他人竞争；
- 底色使用浅海蓝，完成部分使用品牌海洋蓝；
- 可在关键节点放置小岛、旗帜或星星；
- 不使用倒计时制造压力；
- 不因漏学一天清空连续记录。

### 9.8 星级反馈 `rating-stars`

- 仅用于发音闯关的儿童反馈；
- 显示 1–3 颗星，不展示百分制；
- 星星使用行动橙黄；
- 必须同时显示一句可执行的反馈；
- 星级不能与其他学生比较。

### 9.9 信息提示 `notice-info`

用于固定显示：

> 记忆暗号帮助记住意思，正确读音要听标准音。

样式：浅蓝背景、深蓝图标、深色文字；不使用警告黄或错误红。

### 9.10 弹窗 `dialog`

- 仅用于权限、家长验证、敏感操作和需要立即决定的异常；
- 标题简短，正文说明影响和下一步；
- 主操作放右侧或下方最显眼位置；
- 删除数据、支付等敏感操作需要再次确认；
- 儿童学习流程中的普通提示优先使用页面内反馈，不滥用弹窗。

### 9.11 底部导航 `tab-bar`

仅保留两个入口：

- 学习；
- 家长。

规范：

- 当前 Tab 使用海洋蓝图标和文字；
- 未选中使用灰蓝；
- 图标必须配文字；
- “家长”入口不使用锁形图标制造恐惧，可在进入后进行轻量验证。

## 10. 儿童学习模式

儿童模式的关键词是：**少文字、强反馈、大目标、低压力**。

### 10.1 学习首页

页面顺序：

1. 当前教材和单元的弱化标识；
2. 今日航程主卡片；
3. “约 5 分钟、5 个新词、2 个复习词”；
4. 唯一主按钮“开始今日航程”；
5. 本周打卡天数；
6. 当前单元岛屿进度；
7. “学习 / 家长”底部导航。

首页不得出现购买、订单、周报、隐私设置、帮助菜单或复杂课程列表。

### 10.2 标准听音页

视觉优先级：

1. 英文单词；
2. 标准音按钮；
3. 简短动作提示；
4. 航程进度。

首次听音阶段不展示中文谐音。标准音默认播放两遍，并提供手动重听。

### 10.3 谐音联想页

视觉优先级：

1. 插图；
2. 英文单词与中文词义；
3. 简短谐音故事；
4. 标准音按钮；
5. 记忆暗号说明。

故事应控制在一至两句，不需要滚动阅读。

### 10.4 词义验证页

- 优先使用图片选择或听音选图；
- 每屏最多四个选项；
- 选项顺序可变化，但页面结构保持稳定；
- 回答错误后重新突出联想画面，再允许选择；
- 不弹出惩罚性失败页面。

### 10.5 发音闯关页

儿童端只显示：

- 1–3 颗星；
- 一句纠音建议；
- 标准音重听；
- 慢速音频；
- 再试一次或继续航行。

第三次仍未通过时，主操作切换为“继续航行”，系统将该词安排到稍后复习。

### 10.6 航程完成页

允许比其他页面更有表现力，但庆祝不能阻碍退出。

推荐内容：

- 岛屿推进一小段；
- 星星飞入进度；
- 解锁插画碎片或单元徽章；
- 一句具体表扬；
- 明确显示“今天完成了”；
- 主按钮“休息一下”。

不默认展示“再学一轮”。

## 11. 家长模式

家长模式的关键词是：**可靠、清楚、可行动**。

### 11.1 视觉差异

家长区继续使用同一色彩、字体和圆角系统，但需要：

- 降低角色和星星的出现频率；
- 减少大面积插画；
- 提高数据和说明的排版密度；
- 使用更稳定的白色卡片和浅灰蓝背景；
- 避免儿童化标题和游戏化文案。

### 11.2 周报结构

按照家长采取行动的顺序排列：

1. 本周一句总结；
2. 学习天数和任务完成率；
3. 已掌握词数与待复习词数；
4. 发音变化和常见问题；
5. 一条积极反馈；
6. 一条下周建议。

首版使用数字、短进度条和简单趋势，不使用雷达图、班级百分位或复杂仪表盘。

### 11.3 设置与购买

- 教材版本、年级、学期和权益范围必须完整写明；
- 购买按钮只出现在家长区；
- 明确展示“一次性购买、不自动续费”；
- 不使用倒计时、限时消失、从众提示或焦虑文案；
- 支付、教材修改和数据删除必须再次确认。

## 12. 动效规范

### 12.1 动效目的

动效只承担三种作用：

1. 表明操作已被接收；
2. 表明状态发生变化；
3. 表明航程获得推进。

### 12.2 时长

| 动效 | 建议时长 |
|---|---:|
| 按钮按压 | 100–150ms |
| 选项确认 | 180–250ms |
| 页面切换 | 200–300ms |
| 星星奖励 | 500–800ms |
| 单元完成庆祝 | 不超过 1500ms |

### 12.3 动效规则

- 动画不能阻塞下一步操作。
- 页面切换保持短、稳、方向一致。
- 不使用剧烈震动、频繁闪烁或长时间循环动画。
- 错误反馈可使用一次轻微左右位移，不连续抖动。
- 应支持减少动态效果；关闭动画后，信息仍然完整可理解。

## 13. 触控、响应式与安全区

- 普通点击区域最小 44×44px。
- 儿童核心操作建议不小于 52×52px。
- 选项之间至少保留 8px 间距，避免误触。
- 重要操作不得仅依赖滑动或长按完成。
- 适配常见微信小程序手机宽度，重点验证 320–430px 视口。
- 小屏适配顺序：先减少装饰留白，再缩小插画，最后才缩小文字。
- 英文单词字号不应低于 32px，儿童正文不应低于 16px。
- 底部按钮和 Tab Bar 必须适配设备安全区。
- 键盘弹出时，输入框和确认按钮不得被遮挡。

## 14. 无障碍与儿童可用性

- 文字与背景保持足够对比度，常规文字满足 WCAG AA。
- 状态不能只依赖颜色，必须同时使用图标、文字或形状。
- 音频播放状态应有视觉反馈。
- 所有图标按钮提供可读标签。
- 避免快速闪烁和高频循环动画。
- 录音、网络和权限异常不计为学习失败。
- 麦克风授权说明使用儿童能理解的短句，并提供家长可查看的完整说明。
- 中途退出时保存断点，返回后清楚说明“从这里继续”。

## 15. 文案语气

### 15.1 儿童端

语气应短、具体、鼓励行动：

- 使用“听一听”“选一选”“跟着读”等动作词；
- 一句话只表达一个要求；
- 避免抽象术语，例如“语义辨识”“发音准确率”；
- 表扬具体行为，不笼统评价孩子能力。

推荐：

- “最后一个音读得更清楚了。”
- “你记住了这个词的意思。”
- “今天的航程完成啦。”

避免：

- “你真聪明。”
- “你的水平超过了很多同学。”
- “不完成就会失去奖励。”

### 15.2 家长端

语气应客观、平静、可行动：

- 说明观察到的事实；
- 区分学习问题和设备、网络问题；
- 不制造落后焦虑；
- 不将孩子与其他学生比较。

## 16. 加载、空状态和异常状态

### 16.1 加载

- 学习开始前预载今日文字、图片和标准音频；
- 加载时优先显示页面骨架和明确文案；
- 不使用无意义的长循环角色动画掩盖等待。

### 16.2 弱网

推荐文案：

> 网络有点慢，听音和选图还能继续。发音结果会稍后上传。

弱网状态不能阻断已经缓存的学习内容。

### 16.3 录音异常

环境噪声、无有效语音、服务超时和权限问题统一与学习低分区分。

推荐文案：

> 这次没有听清，我们再试一次。

同时说明可能原因和可执行动作，例如靠近麦克风或换到安静位置。

### 16.4 空状态

空状态应包含：

- 一张简单插图或图标；
- 一句状态说明；
- 一个明确下一步。

不得只显示“暂无数据”。

## 17. 设计资产与内容边界

- 学生端视觉不得写死“湘少版”“三年级”或固定单元数量。
- 岛屿、徽章和地图采用可替换主题资产。
- 插画资产应记录版本、用途、适用年龄和审核状态。
- 教材内容、品牌名称和配套音频必须确认授权范围。
- 产品使用自制插画或合法授权素材。
- 初中阶段不得直接沿用低龄角色比例、文案和奖励强度，应重新验证。

## 18. 设计交付要求

每个页面设计稿至少覆盖：

- 默认状态；
- 按下或选中状态；
- 正确反馈；
- 错误或重试反馈；
- 加载状态；
- 弱网或服务异常；
- 小屏适配；
- 长文案或英文长单词；
- 减少动态效果后的静态表现。

核心流程原型应至少覆盖：

1. 新用户完成教材设置；
2. 学习首页开始今日航程；
3. 标准听音；
4. 谐音图片联想；
5. 词义验证；
6. 发音闯关及三次未通过；
7. 中途退出并恢复；
8. 航程完成；
9. 家长进入周报；
10. 家长修改教材或执行敏感操作。

## 19. UI 验收清单

### 19.1 儿童学习界面

- [ ] 当前屏是否只有一个主要任务？
- [ ] 主按钮是否唯一且足够醒目？
- [ ] 英文单词和标准音是否优先于冒险装饰？
- [ ] 儿童是否无需阅读长说明即可操作？
- [ ] 触控目标是否不小于 44×44px？
- [ ] 错误反馈是否提供下一步，而不是只判错？
- [ ] 谐音页面是否始终能重听标准音？
- [ ] 动画是否可以跳过且不阻塞流程？
- [ ] 完成页是否明确告诉孩子今天已经结束？
- [ ] 页面是否没有购买、排名和催付信息？

### 19.2 家长界面

- [ ] 数据是否能在数秒内理解？
- [ ] 是否只展示家长可采取行动的信息？
- [ ] 是否避免与其他孩子比较？
- [ ] 购买范围和续费方式是否清楚？
- [ ] 敏感操作是否经过家长确认？
- [ ] 设备或服务异常是否与学习问题明确区分？

### 19.3 视觉一致性

- [ ] 暖色是否只用于行动、奖励和推进？
- [ ] 标准音入口是否统一使用海洋蓝？
- [ ] 是否遵守统一的字体、圆角和间距 Token？
- [ ] 单页高饱和颜色是否控制在三种以内？
- [ ] 是否避免无意义阴影、渐变和装饰？
- [ ] 状态是否同时通过颜色和非颜色信息表达？

## 20. 参考设计语言

本规范参考并重新适配了以下设计语言：

- Nintendo 2001：暖色表示前进、角色承载趣味、清楚的奖励反馈。
- Apple：单一主操作、低视觉噪声、充足留白、有限颜色、少阴影和不低于 44px 的触控目标。

参考仅用于建立设计原则，不复制其品牌资产、商标、角色或具体页面。

