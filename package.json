{
  "name": "k12-english-word-voyage",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node scripts/build.mjs",
    "build:admin": "vite --config vite.admin.config.ts build",
    "check": "npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "esbuild": "^0.25.0",
    "miniprogram-api-typings": "^4.0.0",
    "typescript": "^5.9.0",
    "vite": "^6.4.3",
    "vitest": "^3.2.0"
  },
  "dependencies": {
    "@cloudbase/js-sdk": "^3.10.1",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  }
}
